const fs=require('fs');const path=require('path');
const DIST=path.join(__dirname,'..','dist');
const SITE='https://www.ruletadecomida.es';
const MOMENTS=[['desayuno',20],['almuerzo',20],['merienda',20],['cena',20]];
const slug=s=>String(s).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
function run(){if(!fs.existsSync(DIST))throw new Error('dist no existe');let guides=0,bad=[];const cities=fs.readdirSync(DIST,{withFileTypes:true}).filter(e=>e.isDirectory()&&!e.name.startsWith('.')&&!['assets','css','js','images'].includes(e.name));for(const city of cities){for(const [moment,expected] of MOMENTS){const dir=path.join(DIST,city.name,moment);if(!fs.existsSync(dir)){bad.push(`${city.name}/${moment}: falta categoria`);continue}const foods=fs.readdirSync(dir,{withFileTypes:true}).filter(e=>e.isDirectory());if(foods.length!==expected)bad.push(`${city.name}/${moment}: ${foods.length}/${expected} comidas`);for(const food of foods){const file=path.join(dir,food.name,'index.html');if(!fs.existsSync(file)){bad.push(`${city.name}/${moment}/${food.name}: 404`);continue}const html=fs.readFileSync(file,'utf8');const canonical=`${SITE}/${city.name}/${moment}/${food.name}/`;if(!html.includes(canonical)||!html.includes('Guía de comida ·')||html.includes('20 ideas para'))bad.push(`${city.name}/${moment}/${food.name}: no parece guía individual`);guides++}}}if(guides!==6320||bad.length)throw new Error(`VERIFICACIÓN FALLIDA: ${guides}/6320 guías; ${bad.length} problemas. ${bad.slice(0,10).join(' | ')}`);console.log('[verify-guide-routes] OK: 6320 guías individuales válidas, 0 errores.')}if(require.main===module)run();module.exports={run};