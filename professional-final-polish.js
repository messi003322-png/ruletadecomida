const fs=require('fs');const path=require('path');
const OUT=path.join(process.cwd(),'dist');const SITE='https://www.ruletadecomida.es';
if(!fs.existsSync(OUT))throw new Error('dist not found');
function walk(d,cb){for(const e of fs.readdirSync(d,{withFileTypes:true})){const f=path.join(d,e.name);e.isDirectory()?walk(f,cb):/\.html$/i.test(e.name)&&cb(f)}}
function cleanTitle(s){return s.replace(/\s+/g,' ').trim()}
function meta(html,file){const rel=path.relative(OUT,file).replace(/\\/g,'/');const slug=rel.replace(/\/index\.html$/i,'').replace(/\.html$/i,'').split('/').filter(Boolean).pop()||'';const words=slug.replace(/[-_]+/g,' ').split(' ').filter(Boolean);let subject=words.slice(0,5).map(w=>w.charAt(0).toUpperCase()+w.slice(1)).join(' ');if(!subject)subject='Qué comer hoy';let title=`${subject} | Ruleta de Comida`;if(!/<title>[^<]{5,120}<\/title>/i.test(html))html=html.replace(/<head([^>]*)>/i,`<head$1><title>${title}</title>`);if(!/<meta[^>]+name=["']description["']/i.test(html))html=html.replace(/<head([^>]*)>/i,`<head$1><meta name="description" content="Descubre qué comer hoy con Ruleta de Comida. Ideas de comida rápidas, variadas y fáciles para decidir sin pensar demasiado.">`);if(!/<link[^>]+rel=["']canonical["']/i.test(html))html=html.replace(/<head([^>]*)>/i,`<head$1><link rel="canonical" href="${SITE}/${rel.replace(/\/index\.html$/i,'').replace(/\.html$/i,'')}">`);return html}
function css(html){if(/professional-final-polish-css/i.test(html))return html;const s=`<style id="professional-final-polish-css">html{scroll-behavior:smooth}body{overflow-x:hidden}main{padding-bottom:48px}h1,h2,h3{letter-spacing:-.02em}p{line-height:1.65}.container,.content,.page-content{max-width:1180px;margin-left:auto;margin-right:auto}.card,.topic-card,.guide-card{transition:transform .18s ease,box-shadow .18s ease}.card:hover,.topic-card:hover,.guide-card:hover{transform:translateY(-2px);box-shadow:0 10px 28px rgba(0,0,0,.07)}a,button{transition:opacity .18s ease,transform .18s ease}button:active,a:active{transform:translateY(1px)}img{max-width:100%;height:auto}@media(max-width:767px){main{padding-bottom:32px}h1{font-size:clamp(1.9rem,8vw,2.6rem)}h2{font-size:clamp(1.35rem,6vw,1.8rem)}p{line-height:1.6}.card,.topic-card,.guide-card{margin-bottom:14px}}</style>`;return html.replace(/<\/head>/i,s+'</head>')}
function removeShortcuts(html){return html.replace(/<section\b[^>]*>(?:(?!<\/section>)[\s\S])*Atajos populares(?:(?!<\/section>)[\s\S])*<\/section>/gi,'')}
let pages=0;walk(OUT,f=>{let h=fs.readFileSync(f,'utf8');h=removeShortcuts(h);h=meta(h,f);h=css(h);fs.writeFileSync(f,h,'utf8');pages++});console.log(`Professional final polish: ${pages} HTML pages optimized.`)
