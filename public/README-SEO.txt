SEO deployment notes

The build process enhances every existing HTML page with canonical metadata, descriptions, Open Graph tags, robots directives, JSON-LD and a generated sitemap. It also preserves the existing static pages and removes the known ad-provider scripts from the source archive.

New landing pages should only be created when they contain genuinely useful, distinct content for a specific search intent. Avoid mass-generated doorway or duplicate pages.
