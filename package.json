{
  "name": "ruletadecomida",
  "private": true,
  "description": "Ruleta de Comida — sitio estático publicado desde el ZIP proporcionado",
  "version": "2026.08.24",
  "scripts": {
    "build": "node static-build.js && node scripts/generate-meal-layers.js && node scripts/ensure-guide-routes.js"
  },
  "dependencies": {
    "adm-zip": "^0.5.16"
  }
}
